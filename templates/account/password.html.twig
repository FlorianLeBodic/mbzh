{% extends 'base.html.twig' %}

{% block title %}MBZH - Modifier mon mot de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe </h1>
    <a href="{{ path('account') }}">Retourner à mon compte</a><br>
    <hr>
    {% if notification != null %}
    <div class="alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
    <hr>
{% endblock %}